---
"tao": minor
---

Changed undecorated macOS windows to keep native rounded corners and shadows by default, and added `with_rounded_corners` and `set_rounded_corners` APIs for opting into the previous raw borderless behavior.
